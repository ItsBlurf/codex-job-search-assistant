# CV

`main_example.tex` is the stock LaTeX CV template. Generated tailored CVs should be saved as `.tex` files here or under the relevant application archive.

Compile with:

```bash
python tools/compile_latex.py cv/main_example.tex
```

Generated PDFs are ignored by git.

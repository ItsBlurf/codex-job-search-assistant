# Job Scraper State

- `seen_jobs.json` stores deduplication state.
- `results/` stores search outputs from `job-scrape`.
- `search-queries.md` stores configured search terms and locations.

Do not bypass anti-bot protections. If a portal blocks automated access, save pasted job descriptions as the fallback.

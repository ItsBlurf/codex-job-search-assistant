# Cover Letters

`cover.cls` and `example_cover_letter.tex` define the stock cover-letter structure. Generated letters should remain grounded in `profile/` and the job posting.

Cover letters usually compile with `xelatex` because the template uses custom fonts.

Generated PDFs are ignored by git.
